Swedish: Mål med övningen: Träna på att lyfta state (State Lifting), skicka funktioner som
callbacks via props, hantera kontrollerade komponenter och arbeta med dynamiska
arrayer (.map() och .filter()) i en förälderkomponent (App).
